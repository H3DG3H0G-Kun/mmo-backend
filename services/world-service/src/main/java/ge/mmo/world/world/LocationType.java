package ge.mmo.world.world;

/** What kind of place a Location is (drives client rendering + "inTown" ambient). */
public enum LocationType {
    TOWN,
    LANDMARK,
    SACRED,
    PLACE_OF_POWER,
    WILDS
}
